import styled from 'styled-components'

export default styled.div`
  font-size: 1.5rem;
  width: 1em;
  height: 1em;
  user-select: none;
  flex-shrink: 0;
  overflow: hidden;
  & svg {
    display: block;
  }
`

export type P = {
  id: string
}

export const enum Gender {
  female,
  male,
  unisex
}

export const enum MainTag {
  new,
  stock,
  top
}

export const enum Category {
  backpack,
  bag,
  suitcase,
  wallet,
  other
}
